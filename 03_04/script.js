var a = 5;
var b = "4";
var sum = a + b; // number is converted to string.
var product = a * b // assumes that b is a number
var diff = a - b // same
var quotiont = a / b // same

var c = "brah";
var sum2 = a + c; // concatinates a and c
var product2 = a * c // should say NaN (not a number)